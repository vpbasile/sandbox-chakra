import { ChevronDownIcon } from '@chakra-ui/icons';
import { Button, ButtonGroup, Menu, MenuButton, MenuItem, MenuList } from '@chakra-ui/react';
import { Link } from 'react-router-dom';
import ColorModeButton from '../helpers/colorModeButton';
import { routeType } from '../typeRoute';

export default function Nav(props: { routesList: routeType[] }) {




    return (navSelect)
}